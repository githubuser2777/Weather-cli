---
phase: 01-unit-testing-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/display/display.go
  - internal/config/config_test.go
  - internal/location/location_test.go
  - internal/weather/weather_test.go
  - internal/display/display_test.go
autonomous: true
user_setup: []
must_haves:
  truths:
    - all tests pass under go test ./...
  artifacts:
    - internal/config/config_test.go
    - internal/location/location_test.go
    - internal/weather/weather_test.go
    - internal/display/display_test.go
  key_links: []
---

# Phase 1 Plan 01: Core Unit Testing & Dynamic Visual Width

## Objective

- **What:** Write comprehensive unit tests for all internal packages and update terminal layout to dynamically adapt to long city names without breaking the box outline.
- **Why:** Establishes code robustness, ensures regression safety, and guarantees that visual rendering remains aligned under all location lengths.
- **Output:** Unit tests for config, location, weather, and display packages, plus a dynamically resizing display box.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Implement config unit tests</name>
  <files>
    - internal/config/config_test.go
  </files>
  <action>Write unit tests for LoadConfig, SaveConfig, default fallbacks, and temporary file path resolutions using t.TempDir().</action>
  <verify>Run go test -v ./internal/config</verify>
  <done>All config tests pass cleanly without hitting actual user config files.</done>
</task>

<task type="code">
  <name>Implement location geocoding unit tests with mock server</name>
  <files>
    - internal/location/location_test.go
  </files>
  <action>Write unit tests for DetectLocation using net/http/httptest to mock the JSON responses from ip-api.com, verifying successful parsing and proper timeout error handling.</action>
  <verify>Run go test -v ./internal/location</verify>
  <done>DetectLocation tests pass under mock endpoints.</done>
</task>

<task type="code">
  <name>Implement weather fetching unit tests with mock server</name>
  <files>
    - internal/weather/weather_test.go
  </files>
  <action>Write unit tests for FetchWeather, GeocodeCity, and table-driven tests for decodeWeatherCode. Mock Open-Meteo responses using net/http/httptest.</action>
  <verify>Run go test -v ./internal/weather</verify>
  <done>All weather tests pass using mocked payloads.</done>
</task>

<task type="code">
  <name>Implement dynamic box width calculation in display</name>
  <files>
    - internal/display/display.go
  </files>
  <action>Refactor RenderWeather to dynamically calculate boxWidth based on the maximum visual length of rows to prevent display border breakage for long location names. Use unicode/utf8.RuneCountInString to count lengths accurately.</action>
  <verify>Run local weather cli with a very long city name or run tests.</verify>
  <done>Long strings fit inside properly formatted, closed widget borders.</done>
</task>

<task type="code">
  <name>Implement display formatting unit tests</name>
  <files>
    - internal/display/display_test.go
  </files>
  <action>Write tests for formatTemp, formatHumidity, formatConditions, and layout calculation functions in display.</action>
  <verify>Run go test -v ./internal/display</verify>
  <done>Display tests pass.</done>
</task>

## Verification

- Run `go test -v ./...` in the root workspace directory.
- Verify the CLI box rendering with standard and extra-long inputs.

## Success Criteria

- [ ] All package unit tests pass.
- [ ] Mock HTTP servers successfully simulate network conditions for weather and location modules.
- [ ] No live network requests are made during test execution.
- [ ] CLI box display accommodates long city names without breaking the border outline.
